apiVersion: v1
kind: Service
metadata:
  labels:
    app: frontend
  name: frontend-service
spec:
  type: LoadBalancer
  ports:
    - port: 80
      nodePort: 31000
      protocol: TCP
      targetPort: 80
  selector:
    app: frontend
status:
  loadBalancer: {}
